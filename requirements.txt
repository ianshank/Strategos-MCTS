# Demo/E2E dependency manifest.
# Consumers: .github/workflows/docker-deployment.yml (installed unguarded after
# pip install -e ., deliberately capping numpy < 2 for the sanity checks) and
# e2e_with_langsmith.yml (guarded install). Not an extras replacement — several
# pins here (selenium, webdriver-manager, wandb) have no pyproject extra home.

# Core UI Framework
gradio>=4.0.0,<6.0.0  # matches pyproject [ui]. The two files previously disagreed (<5 here, <6 there)
                      # while the comment claimed this was "the major app.py is exercised against" —
                      # CI resolves 5.50.0. src/ui/gradio_compat.py detects gr.Timer vs
                      # Blocks.load(every=) at runtime, so both majors work and the ui-tests job
                      # exercises whichever one resolves.

# Numerical computation
numpy>=1.24.0,<2.0.0

# Machine Learning - Neural Models
# NOTE: peft 0.10.0 is compatible with transformers 4.40.x
# Newer peft versions (0.14+) require transformers>=4.46 for modeling_layers module
torch>=2.1.0
transformers>=4.40.0,<4.46.0
peft==0.20.0
accelerate>=1.14.0
sentence-transformers>=2.2.0

# Configuration
pyyaml>=6.0

# Experiment Tracking
wandb>=0.16.0

# Required for Gradio OAuth and model loading
huggingface_hub>=0.20.0,<0.30.0

# LangSmith tracing (used across smoke/component tests)
langsmith>=0.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Chess support
python-chess>=1.10.0

# Selenium testing (optional - for UI tests)
selenium>=4.46.0
webdriver-manager>=4.0.0

# Note: This demo now uses REAL trained models:
# - RNN Meta-Controller (models/rnn_meta_controller.pt)
# - BERT with LoRA adapters (models/bert_lora/final_model/)
# - Actual HRM and TRM agent implementations
